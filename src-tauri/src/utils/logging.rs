// ============================================================
// 日志初始化（tracing）—— v0.1 用默认配置（info 级别）
// ============================================================

pub fn init() {
    let _ = tracing_subscriber::fmt().with_target(false).try_init();
}
