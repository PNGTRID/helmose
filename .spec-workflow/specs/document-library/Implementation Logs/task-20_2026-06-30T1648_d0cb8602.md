# Implementation Log: Task 20

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：render_markdown 与 incremental 逻辑的单元测试（tasks.md backlog 标 [x] 已补）。覆盖范围：(1) commands/library.rs:457 #[cfg(test)] mod tests —— 4 个测试：wikilink_basic_replaced（[[x]] 与 [[x|alias]] 替换为 <a data-target>）、wikilink_skipped_in_code_fence（fenced code 内 [[y]] 字面不替换）、markdown_renders_wikilink_as_link（render_markdown 全链断言 html 含 helmose-wikilink class）、save_note_content_backs_up_and_reindexes（集成测试：临时 vault → index_vault_inner → save_note_content → 验证 .helmose/backup + FTS 命中 + raw_content 更新）。(2) services/indexer/incremental.rs:247 #[cfg(test)] mod tests —— upsert_then_search_then_remove：临时 vault + Database + init_schema → upsert_file 写新文件 → 断言 FTS MATCH '会员系统' 命中 → upsert_rel 改内容 → 断言新关键词命中 → remove_file → 断言 FTS 命中数为 0。两套测试都用临时目录 + 临时 db（pid+uuid 命名避并发撞库），不依赖 ~/wiki 真实 vault，CI 可复现。整体让 cargo test 达 18 passed。

**Timestamp:** 2026-06-30T16:48:02.095Z
**Log ID:** d0cb8602-7785-4a64-8558-9bf90df72d6f

---

## Statistics

- **Lines Added:** +150
- **Lines Removed:** -0
- **Files Changed:** 2
- **Net Change:** 150

## Files Modified
- src-tauri/src/commands/library.rs
- src-tauri/src/services/indexer/incremental.rs

## Files Created
_No files created_

---

## Artifacts

### Functions

#### render_markdown
- **Purpose:** 被测函数：pulldown-cmark 渲染 md→HTML（GFM 表格/任务列表/删除线），渲染前先 render_wikilinks 预处理
- **Location:** src-tauri/src/commands/library.rs:386
- **Signature:** fn render_markdown(md: &str) -> String
- **Exported:** No

#### render_wikilinks
- **Purpose:** 被测函数：[[x]] / [[x|y]] 预处理为可点 <a data-target>，跳过 fenced code block
- **Location:** src-tauri/src/commands/library.rs:422
- **Signature:** fn render_wikilinks(md: &str) -> String
- **Exported:** No

#### upsert_file
- **Purpose:** 被测函数：增量索引单文件 upsert（绝对路径版）→ 写库 + FTS 单行同步
- **Location:** src-tauri/src/services/indexer/incremental.rs:38
- **Signature:** pub fn upsert_file(db: &Database, vault_id: &str, root: &Path, abs: &Path) -> Result<bool, String>
- **Exported:** No

#### upsert_rel
- **Purpose:** 被测函数：增量索引单文件 upsert（相对路径+内容版，编辑场景）→ FTS 单行同步新内容
- **Location:** src-tauri/src/services/indexer/incremental.rs:49
- **Signature:** pub fn upsert_rel(db: &Database, vault_id: &str, rel: &str, content: &str, abs: Option<&Path>) -> Result<bool, String>
- **Exported:** No

#### remove_file
- **Purpose:** 被测函数：增量索引单文件删除 → notes 行删除 + FTS 单行清理
- **Location:** src-tauri/src/services/indexer/incremental.rs:209
- **Signature:** pub fn remove_file(db: &Database, vault_id: &str, root: &Path, abs: &Path) -> Result<bool, String>
- **Exported:** No

