# Implementation Log: Task 16

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：标签侧栏功能。后端 commands/notes.rs::get_tags_stats 聚合全库 tags 出现次数（按次数倒序返回 Vec<(String,i64)>）；tags 在 notes 表存为 JSON 数组，SQL 难直接 GROUP BY，故由 Rust 端聚合——只读 tags 列（轻量，不读 raw_content），serde_json 反序列化后 HashMap 计数 + 按值倒序。前端复用方位于 components/SidePanel.tsx（Obsidian 式右侧大纲侧栏，非 LibraryPage 左栏——tasks.md 描述「LibraryPage.tsx 左栏」对当前代码已过时，实际落点 SidePanel）：useState 维护 tags 列表 + 当前查询 tag + 命中笔记 SearchResult[]；useEffect + 竞态保护（tagReqIdRef 单调递增 id）拉 getTagsStats；点击标签 → 调 searchNotes(tag) 走 FTS5 搜 tags 列，结果以 SearchResult 列表形式塞回侧栏面板。tags.slice(0,80) 截顶防 UI 爆。api/index.ts::getTagsStats / types/index.ts::TagCount = [string,number]（对齐 Rust serde tuple 序列化）。

**Timestamp:** 2026-06-30T16:47:30.593Z
**Log ID:** 6395a8de-53ee-446a-b0f2-19ff58319e47

---

## Statistics

- **Lines Added:** +90
- **Lines Removed:** -0
- **Files Changed:** 5
- **Net Change:** 90

## Files Modified
- src-tauri/src/commands/notes.rs
- src-tauri/src/main.rs
- frontend/src/api/index.ts
- frontend/src/types/index.ts
- frontend/src/components/SidePanel.tsx

## Files Created
_No files created_

---

## Artifacts

### API Endpoints

#### GET get_tags_stats
- **Purpose:** 聚合全库 tags 出现次数（按次数倒序），供标签侧栏展示。tags 存 JSON 数组无法直接 SQL GROUP BY，由 Rust 端 HashMap 计数；只读 tags 列不读 raw_content，遵守性能红线
- **Location:** src-tauri/src/commands/notes.rs:83
- **Request Format:** vault_id: String
- **Response Format:** Vec<(String, i64)> —— [(tag, count)]，按 count 倒序；serde 以 JSON 数组元组序列化，前端类型 TagCount = [string, number]

### Components

#### SidePanel
- **Type:** React
- **Purpose:** Obsidian 式右侧大纲/标签侧栏：tags 聚合计数（前 80）+ 点击标签走 FTS5 搜 → 命中笔记 SearchResult 列表。竞态保护 tagReqIdRef，默认展开 tagsOpen
- **Location:** frontend/src/components/SidePanel.tsx
- **Props:** 无（内部 useVaultStore 取 vault；维护 tags/tagQuery/tagResults/tagLoading 状态）
- **Exports:** default

### Functions

#### get_tags_stats
- **Purpose:** Rust 端聚合 tags 计数：SELECT tags FROM notes → serde_json 反序列化 Vec<String> → HashMap 计数 → 按值倒序 sort。空 tag 跳过
- **Location:** src-tauri/src/commands/notes.rs:83
- **Signature:** #[tauri::command] pub fn get_tags_stats(vault_id: String, db: State<'_, Database>) -> Result<Vec<(String, i64)>, String>
- **Exported:** No

