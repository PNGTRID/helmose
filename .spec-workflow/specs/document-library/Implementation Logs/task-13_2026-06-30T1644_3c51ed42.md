# Implementation Log: Task 13

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：增量索引——notify-debouncer-mini 文件监听 + 单文件 upsert/remove + FTS5 单行同步。核心难点是 FTS5 contentless 表的单行维护（不像普通 FTS 表随基表自动同步）：(1) 删除须发 INSERT INTO notes_fts(notes_fts, rowid, title, raw_content, tags) VALUES('delete', ?, ?, ?, ?)——contentless delete 必须提供原始列值（FTS 内部不存内容），故 upsert/remove 前都先 SELECT 旧 rowid+title+raw_content+tags；(2) 插入用 INSERT INTO notes_fts(rowid,...) SELECT ... FROM notes WHERE id=? 从 notes 表读原值（与全量 'rebuild' 同源，保证一致）。upsert_rel 还做 content_hash 碰撞消歧（同 vault 已有同 hash 的别的 rel_path → id = hash#short_hash(rel)），重建 tasks（按 note 删旧重插），重建 links（target 按 <stem>.md 近似解析，全量用全局 stem_to_id，增量近似，下次全量修正）。watcher.rs：独立线程跑 debouncer 600ms 批量，逐路径 handle（非 md 跳过，exists→upsert，不存在→remove），有变更 emit \"vault-changed\" 让前端 store 自增 watcherTick 触发刷新；AtomicBool 幂等守护（v0.1 单 vault，切换 vault 需重启应用，见 task-8 注记的 tabs 架构）。start_watcher 命令（commands/index.rs:257）解 State 取 root_path 后调 services::watcher::start。配套单测 upsert_then_search_then_remove（临时 vault + 真 SQLite，验证 upsert 后 FTS 命中、编辑后新词进入旧词出、remove 后 FTS 无命中）。

**Timestamp:** 2026-06-30T16:44:39.507Z
**Log ID:** 3c51ed42-d513-42d1-b962-87ad56a5a793

---

## Statistics

- **Lines Added:** +360
- **Lines Removed:** -5
- **Files Changed:** 5
- **Net Change:** 355

## Files Modified
- src-tauri/src/commands/index.rs
- src-tauri/src/main.rs
- src-tauri/Cargo.toml

## Files Created
- src-tauri/src/services/indexer/incremental.rs
- src-tauri/src/services/watcher.rs

---

## Artifacts

### API Endpoints

#### GET start_watcher
- **Purpose:** 启动 vault 文件监听（增量索引）。前端 vault 就绪后调用，幕等（v0.1 单 vault）
- **Location:** src-tauri/src/commands/index.rs:257
- **Request Format:** vault_id: String, app: tauri::AppHandle, db: State<Database>
- **Response Format:** Result<(), String>

### Functions

#### upsert_file
- **Purpose:** 读盘 + qualify 判定（非 md / 排除目录 / 不在 root 下 返回 Ok(false)）→ 转调 upsert_rel。watcher 文件改动事件调用
- **Location:** src-tauri/src/services/indexer/incremental.rs:38
- **Signature:** pub fn upsert_file(db: &Database, vault_id: &str, root: &Path, abs: &Path) -> Result<bool, String>
- **Exported:** No

#### upsert_rel
- **Purpose:** 单文件 upsert 核心：事务内 (1) 取旧记录 rowid+原值发 FTS 'delete' (2) content_hash 碰撞检测加 rel_path 短哈希消歧 (3) INSERT notes (4) 从 notes 读原值 INSERT FTS（与全量 'rebuild' 同源）(5) 重建 tasks (6) 重建 links（target 按 <stem>.md 近似解析，下次全量修正）。编辑保存直接调用不读盘
- **Location:** src-tauri/src/services/indexer/incremental.rs:49
- **Signature:** pub fn upsert_rel(db: &Database, vault_id: &str, rel: &str, content: &str, abs: Option<&Path>) -> Result<bool, String>
- **Exported:** No

#### remove_file
- **Purpose:** 文件删除事件 → 转 rel → 调 remove_rel
- **Location:** src-tauri/src/services/indexer/incremental.rs:209
- **Signature:** pub fn remove_file(db: &Database, vault_id: &str, root: &Path, abs: &Path) -> Result<bool, String>
- **Exported:** No

#### remove_rel
- **Purpose:** 单文件 remove：事务内查旧记录 rowid+原值 → 发 FTS 'delete'（须提供原始列值，contentless 表特性）→ DELETE notes。返回是否确实删了一行
- **Location:** src-tauri/src/services/indexer/incremental.rs:218
- **Signature:** pub fn remove_rel(db: &Database, vault_id: &str, rel: &str) -> Result<bool, String>
- **Exported:** No

#### start
- **Purpose:** 启动 watcher：独立线程跑 notify-debouncer-mini，600ms debounce 批量处理，逐路径调 incremental::upsert_file/remove_file，有变更 emit "vault-changed"，AtomicBool 幂等守护（v0.1 单 vault，切换 vault 需重启）
- **Location:** src-tauri/src/services/watcher.rs:23
- **Signature:** pub fn start(app: AppHandle, db: Database, vault_id: String, root: PathBuf)
- **Exported:** No

#### handle
- **Purpose:** 处理单个变化路径：非 md 跳过，exists 走 upsert_file，不存在走 remove_file，返是否改了索引库
- **Location:** src-tauri/src/services/watcher.rs:70
- **Signature:** fn handle(db: &Database, vault_id: &str, root: &Path, path: &Path) -> bool
- **Exported:** No

