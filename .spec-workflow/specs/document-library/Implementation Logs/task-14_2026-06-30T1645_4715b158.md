# Implementation Log: Task 14

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：笔记编辑写回 vault——这是本项目首次“写 vault 原文”的能力，严格遵守架构铁律 2（绝不在用户没要求时改 vault，必须有用户明确动作 + 保护机制）。后端 save_note_content（library.rs:268）为命令壳，转调 save_note_content_inner（library.rs:206，可被集成测试直接调用绕过 Tauri State）。inner 五步：(1) 查 note 所属 vault + rel_path + root_path（JOIN vaults）；(2) 原文件存在则备份——目标 <vault>/.helmose/backup/，文件名 <rel_path 中 / 替换 _>.<iso8601 去冒号>.md，create_dir_all + fs::copy（纳在隐藏目录 .helmose 下，不索引）；(3) create_dir_all 父目录（防止创建子目录笔记时失败）+ fs::write 写新内容到 vault 原文；(4) 调 incremental::upsert_rel 增量重索引（notes + FTS + tasks + links 同步更新，依赖 task-13）；(5) 重新 render_markdown 返回 NoteContent（title 设 None，编辑后标题可能变，前端用 selected 显示）。前端 NoteEditor.tsx（从 NoteView 拆出）：CodeMirror（markdown 语言高亮）+ marked 实时预览分屏 + 保存/取消按钮；useState draft 以 rawContent 初始化，save() 调 api.saveNoteContent(noteId, draft) → message.success 提示「已保存（自动备份到 .helmose/backup）」+ onSave(updated) 回调让父组件切换回阅读视图。配套集成测试 save_note_content_backs_up_and_reindexes（library.rs:486）：临时 vault + 真 SQLite + 真 FS，验证 .helmose/backup/ 有备份 / vault 原文更新 / FTS 命中新内容 / notes.raw_content 更新。

**Timestamp:** 2026-06-30T16:45:02.257Z
**Log ID:** 4715b158-bb98-4d57-86ae-7c212d0d9fbf

---

## Statistics

- **Lines Added:** +130
- **Lines Removed:** -0
- **Files Changed:** 5
- **Net Change:** 130

## Files Modified
- src-tauri/src/commands/library.rs
- frontend/src/components/NoteEditor.tsx
- frontend/src/api/index.ts
- frontend/src/types/index.ts
- src-tauri/src/main.rs

## Files Created
_No files created_

---

## Artifacts

### API Endpoints

#### POST save_note_content
- **Purpose:** 保存笔记内容：写回 vault md 原文 + 写前备份到 <vault>/.helmose/backup/ + 增量重索引（notes + FTS + tasks + links 同步）。用户明确「保存」动作触发，带备份保护，遵守架构铁律 2（绝不在用户没要求时改 vault）
- **Location:** src-tauri/src/commands/library.rs:268
- **Request Format:** note_id: String, content: String
- **Response Format:** NoteContent { id, rel_path, title, raw_content, html }（重新渲染的 HTML）

### Components

#### NoteEditor
- **Type:** React
- **Purpose:** 笔记编辑模式组件：CodeMirror 编辑器 + marked 实时预览分屏 + 保存/取消按钮。保存调 api.saveNoteContent → 成功后回调 onSave(updated) + message.success（提示「已保存（自动备份到 .helmose/backup）」）
- **Location:** frontend/src/components/NoteEditor.tsx
- **Props:** { noteId: string, rawContent: string, onSave: (updated: NoteContent) => void, onCancel: () => void }
- **Exports:** default

### Functions

#### save_note_content_inner
- **Purpose:** 保存核心逻辑（可被集成测试直接调用绕过 Tauri State）：(1) 查 note 所属 vault + rel_path (2) 原文件存在则备份到 <vault>/.helmose/backup/<safe_name>.<iso8601>.md（隐藏目录，不索引） (3) create_dir_all 父目录 + 写新内容到 vault 原文 (4) 调 incremental::upsert_rel 增量重索引（notes + FTS + tasks + links 同步） (5) 返回最新 NoteContent（重新 render_markdown）
- **Location:** src-tauri/src/commands/library.rs:206
- **Signature:** pub fn save_note_content_inner(note_id: &str, content: &str, db: &Database) -> Result<NoteContent, String>
- **Exported:** No

