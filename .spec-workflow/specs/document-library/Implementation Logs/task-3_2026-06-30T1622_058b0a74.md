# Implementation Log: Task 3

**Summary:** 事后补录(2026-07-01,/ruiqin 补 log):新建 src-tauri/src/commands/library.rs,实现本期 4 个 API: list_dirs(目录树扁平路径,walkdir+is_excluded 共用排除规则)、list_notes_meta(按 dir_prefix SQL 过滤+Rust 直接子判定,limit 默认 5000)、get_note_content(单篇全文+渲染 HTML)、内部函数 render_markdown(pulldown-cmark GFM 渲染)。严格只录本期范围——v0.2 后加的 render_wikilinks/list_all_notes_meta/save_note_content/get_backlinks/get_graph_data 不属于 task-3。内部 vault_root 工具函数查 vaults.root_path。所有命令 #[tauri::command] 风格,State<'_, Database> 取库,错误 .map_err(|e| e.to_string())?。满足 Requirements 1.1/1.2/2.1/3.1/3.2/4.1/4.2(排除目录与 index.rs 一致,树视图=索引视图;列表只回元数据,遵守性能红线)。

**Timestamp:** 2026-06-30T16:22:09.515Z
**Log ID:** 058b0a74-8c76-4e2b-8c77-4d16240b4816

---

## Statistics

- **Lines Added:** +200
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 200

## Files Modified
_No files modified_

## Files Created
- src-tauri/src/commands/library.rs

---

## Artifacts

### API Endpoints

#### GET list_dirs
- **Purpose:** 列出 vault 所有目录(相对路径扁平数组,"" = 根),供前端构建 Obsidian 式文件树。WalkDir 扫描,排除规则与 index.rs 一致(树视图=索引视图)
- **Location:** src-tauri/src/commands/library.rs:35
- **Request Format:** vault_id: String
- **Response Format:** Vec<String> (扁平相对路径数组,根用空串)

#### GET list_notes_meta
- **Purpose:** 列出某目录「直接子」的笔记元数据(不含正文)。SQL 按前缀过滤 + Rust 端 strip_prefix 后判定不再含 '/' 实现直接子;limit 默认 5000 防超大目录爆 IPC
- **Location:** src-tauri/src/commands/library.rs:60
- **Request Format:** vault_id: String, dir_prefix: Option<String> (""=根), limit: Option<i64>
- **Response Format:** Vec<NoteMeta> (轻量,无 raw_content/frontmatter)

#### GET get_note_content
- **Purpose:** 取单篇笔记正文 + pulldown-cmark 渲染的 GFM HTML(预览面板用)。返回 NoteContent{raw_content, html}
- **Location:** src-tauri/src/commands/library.rs:170
- **Request Format:** note_id: String
- **Response Format:** NoteContent { id, rel_path, title, raw_content, html }

### Functions

#### render_markdown
- **Purpose:** pulldown-cmark 渲染 md → HTML,启用 GFM 表格/任务列表/删除线。渲染前先调 render_wikilinks 预处理 wikilink(本期 task-3 仅原始 md→html 逻辑,wikilink 预处理是 v0.2 加的,本期函数本体已存在)
- **Location:** src-tauri/src/commands/library.rs:386
- **Signature:** fn render_markdown(md: &str) -> String
- **Exported:** No

#### vault_root
- **Purpose:** 内部工具函数:按 vault_id 查 vaults 表 root_path,返回 PathBuf。供 list_dirs 复用
- **Location:** src-tauri/src/commands/library.rs:19
- **Signature:** fn vault_root(vault_id: &str, db: &Database) -> Result<PathBuf, String>
- **Exported:** No

