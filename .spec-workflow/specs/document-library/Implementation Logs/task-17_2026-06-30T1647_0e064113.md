# Implementation Log: Task 17

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：图谱视图功能（全库双链关系力导向可视化）。后端 commands/library.rs::get_graph_data：先 SELECT source_note_id/target_note_id FROM links（已解析的正向 wikilink，target_note_id NOT NULL）→ HashMap 计节点度数 → sort 按度数倒序 take(limit.unwrap_or(1000)) 取 top N → keep 过滤后构造 edges（Vec<GraphEdge>）+ 动态 IN(?) 查节点元数据构造 nodes。limit 默认 1000（tasks.md 早期写「top 600」是计划值，代码实际默认 1000；ForceGraph 注释「n≤600」指排斥力 O(n²) 的可接受阈值）。models/note.rs 定义 GraphNode/GraphEdge/GraphData。前端 pages/GraphPage.tsx 拉取数据（依赖 vault.id + watcherTick 增量重拉）→ DataState 三态 + children 用 {data && …} 短路保护防加载期 null 崩溃（c6778fa 修过的回归）；点击节点 onSelect → openNoteFromMeta。components/ForceGraph.tsx 自绘 canvas 力导向（不引第三方依赖避免构建不确定性）：三力（中心引力 + 节点排斥 O(n²) + 边弹簧）+ 阻尼，DPR 适配高分辨率屏，节点半径按度数渐变，mousedown/move/up 拖拽 + <5px 视为点击。

**Timestamp:** 2026-06-30T16:47:40.542Z
**Log ID:** 0e064113-9527-43bd-89b1-5352279fc425

---

## Statistics

- **Lines Added:** +320
- **Lines Removed:** -0
- **Files Changed:** 9
- **Net Change:** 320

## Files Modified
- src-tauri/src/commands/library.rs
- src-tauri/src/models/note.rs
- src-tauri/src/main.rs
- frontend/src/api/index.ts
- frontend/src/types/index.ts
- frontend/src/pages/GraphPage.tsx
- frontend/src/components/ForceGraph.tsx

## Files Created
- frontend/src/pages/GraphPage.tsx
- frontend/src/components/ForceGraph.tsx

---

## Artifacts

### API Endpoints

#### GET get_graph_data
- **Purpose:** 图谱数据：nodes + edges，按度数 top N 截断防超大库卡前端。limit 默认 1000
- **Location:** src-tauri/src/commands/library.rs:316
- **Request Format:** vault_id: String, limit: Option<i64> (默认 1000)
- **Response Format:** GraphData { nodes: Vec<GraphNode>, edges: Vec<GraphEdge> }

### Components

#### GraphPage
- **Type:** React
- **Purpose:** 图谱视图页：拉 getGraphData → DataState 三态 + {data && …} 短路防 null 崩溃 → 点击节点 openNoteFromMeta 跳转
- **Location:** frontend/src/pages/GraphPage.tsx
- **Props:** 无（useVaultStore 取 vault + watcherTick）
- **Exports:** default

#### ForceGraph
- **Type:** React
- **Purpose:** 轻量自绘 canvas 力导向：中心引力 + 节点排斥 O(n²) + 边弹簧 + 阻尼；DPR 适配，拖拽重排，<5px 视为点击跳转
- **Location:** frontend/src/components/ForceGraph.tsx
- **Props:** data: GraphData, onSelect: (n: GraphNode) => void
- **Exports:** default

### Classes

#### GraphData
- **Purpose:** 图谱数据 DTO：nodes + edges（按度数 top N 截断）
- **Location:** src-tauri/src/models/note.rs:96
- **Exported:** No

#### GraphNode
- **Purpose:** 图谱节点
- **Location:** src-tauri/src/models/note.rs:81
- **Exported:** No

#### GraphEdge
- **Purpose:** 图谱边（source → target，已解析的正向 wikilink）
- **Location:** src-tauri/src/models/note.rs:89
- **Exported:** No

