# Implementation Log: Task 12

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：wikilink [[x]] 双链正向跳转。后端：render_wikilinks（library.rs:422）在 render_markdown 调 pulldown-cmark 前预处理正文——逐行扫描，遇到 ``` / ~~~ 切换 in_fence 状态（fenced code block 内的字面 [[x]] 不替换，避免污染代码），行外用正则 RE_WIKILINK_RENDER (\\[\\[([^\\]|]+)(?:\\|([^\\]]+))?\\]\\]) 替换 [[target]] / [[target|alias]] 为 <a class=\"helmose-wikilink\" data-target=\"<esc_attr(target)\"><esc_html(alias)></a>。target 支持 #anchor 前缀剥离（split('#').next()），空 target（如 [[#anchor]]）原样保留。esc_attr 防 data-target 引号注入，esc_html 转义 alias 显示文本。pulldown-cmark 默认透传 inline HTML，故替换后正常 markdown 渲染即可产出可点链接。前端：useWikilinkNavigation hook（frontend/src/hooks/useWikilinkNavigation.ts）统一处理点击——closest('.helmose-wikilink') 取 dataset.target，调 api.searchNotes(vault.id, name, 1) 复用 FTS5 查目标，命中第 1 条则 openNoteFromMeta 跳转（默认）或调 onNavigate 回调（TasksPage Drawer 替换内容用）；失败静默、未命中不报错（行为对齐现状）。原 LibraryPage.onPreviewClick 随 v0.2 重构迁出，现 NotePreview 与 TasksPage 共用此 hook（消除重复）。配套 3 个单测：wikilink_basic_replaced（基本替换 + alias）、wikilink_skipped_in_code_fence（代码块跳过）、markdown_renders_wikilink_as_link（端到端渲染检查）。

**Timestamp:** 2026-06-30T16:44:14.224Z
**Log ID:** 310928a9-815e-4a65-ace8-290709abd31a

---

## Statistics

- **Lines Added:** +130
- **Lines Removed:** -5
- **Files Changed:** 4
- **Net Change:** 125

## Files Modified
- src-tauri/src/commands/library.rs
- frontend/src/components/NotePreview.tsx
- frontend/src/pages/TasksPage.tsx

## Files Created
- frontend/src/hooks/useWikilinkNavigation.ts

---

## Artifacts

### Components

#### NotePreview
- **Type:** React
- **Purpose:** 笔记预览组件：渲染后端返回的 HTML（含已预处理的可点 wikilink <a>），通过 useWikilinkNavigation hook 绑定点击事件，命中后默认行为 openNoteFromMeta 跳转目标笔记
- **Location:** frontend/src/components/NotePreview.tsx
- **Props:** { html?: string, rawContent?: string, onNavigate?: (hit) => void }（实际签名以代码为准）
- **Exports:** default

### Functions

#### render_wikilinks
- **Purpose:** 把正文里的 wikilink [[x]] / [[x|y]] 预处理为可点 <a class="helmose-wikilink" data-target="...">。逐行扫描跳过 ``` / ~~~ fenced code block（代码块内字面 [[x]] 不替换），支持 #anchor 前缀剥离。pulldown-cmark 默认透传 inline HTML，故替换后再走 markdown 渲染即可
- **Location:** src-tauri/src/commands/library.rs:422
- **Signature:** fn render_wikilinks(md: &str) -> String
- **Exported:** No

#### esc_attr
- **Purpose:** HTML 属性值转义（& " <），防注入 / 防破坏 data-target 引号
- **Location:** src-tauri/src/commands/library.rs:406
- **Signature:** fn esc_attr(s: &str) -> String
- **Exported:** No

#### esc_html
- **Purpose:** HTML 文本转义（& < >），用于 alias 显示文本
- **Location:** src-tauri/src/commands/library.rs:413
- **Signature:** fn esc_html(s: &str) -> String
- **Exported:** No

