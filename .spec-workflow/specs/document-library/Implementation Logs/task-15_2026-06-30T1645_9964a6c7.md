# Implementation Log: Task 15

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：反向链接面板——基于 links 表的反向查询。后端 get_backlinks（library.rs:278）JOIN links + notes 查 `links.target_note_id = ?` （即「谁链接了本笔记」），返回 Vec<Backlink>，按 n.date_iso DESC 排序（最新的源笔记优先）。Backlink DTO = { source: NoteMeta, target_text: String, alias: Option<String> }——复用 NoteMeta 轻量元数据（不带 raw_content，性能红线），target_text/alias 来自 links 表（保存源笔记里写的 [[target]] 或 [[target|alias]] 原文）。关键设计：links 表由 indexer 解析 wikilink 时写入（task-3 全量 + task-13 增量），target_note_id 在全量索引用全局 stem_to_id 解析，增量用 <stem>.md 近似解析（下次全量修正）；dangling 链接（target_note_id IS NULL）不会出现在反向链接里。前端落点：NoteView.tsx（useEffect 监听 activeNoteId 变化 → api.getBacklinks(noteId).catch(() => []) → 写入 useTabsStore.activeBacklinks，出错静默空数组），SidePanel.tsx 右侧面板从 store 读 activeBacklinks 渲染「反向链接（N）」标题 + 列表，空态显示「打开一篇笔记后，此处显示其大纲与反向链接」占位。点击反向链接项跳转源笔记（复用 openNoteFromMeta）。这是 Obsidian 式双链体验的「被引用」侧，与 task-12 的「正向跳转」互补。

**Timestamp:** 2026-06-30T16:45:34.211Z
**Log ID:** 9964a6c7-16b1-493e-b703-ac7aef417e8d

---

## Statistics

- **Lines Added:** +90
- **Lines Removed:** -0
- **Files Changed:** 9
- **Net Change:** 90

## Files Modified
- src-tauri/src/commands/library.rs
- src-tauri/src/models/note.rs
- src-tauri/src/models/mod.rs
- src-tauri/src/main.rs
- frontend/src/components/SidePanel.tsx
- frontend/src/components/NoteView.tsx
- frontend/src/stores/tabs.ts
- frontend/src/types/index.ts
- frontend/src/api/index.ts

## Files Created
_No files created_

---

## Artifacts

### API Endpoints

#### GET get_backlinks
- **Purpose:** 反向链接查询：哪些笔记的 [[wikilink]] 指向了本笔记。JOIN links + notes 查 links.target_note_id = note_id，返回源笔记元数据 + 链接文本，按 date_iso DESC 排序
- **Location:** src-tauri/src/commands/library.rs:278
- **Request Format:** note_id: String
- **Response Format:** Vec<Backlink> { source: NoteMeta, target_text: String, alias: Option<String> }

### Components

#### SidePanel
- **Type:** React
- **Purpose:** 右侧面板组件：显示当前笔记的大纲 + 反向链接（「反向链接（N）」标题 + 列表，点击跳转源笔记）+ 标签。从 useTabsStore.activeBacklinks 读，空状态显示占位提示
- **Location:** frontend/src/components/SidePanel.tsx
- **Props:** {}（无 props，状态从 tabs store 读 activeBacklinks）
- **Exports:** default

#### NoteView
- **Type:** React
- **Purpose:** 笔记阅读视图容器：useEffect 监听 noteId 变化 → api.getBacklinks(noteId).catch(() => []) → useTabsStore.setState({activeBacklinks})。出错静默空数组（与项目一致）
- **Location:** frontend/src/components/NoteView.tsx
- **Props:** {}（状态自管：activeNoteId 从 tabs store）
- **Exports:** default

### Classes

#### Backlink
- **Purpose:** 反向链接 DTO：源笔记元数据（NoteMeta）+ 在源笔记里写的 [[target_text]] 或 [[target_text|alias]] 链接文本。用 NoteMeta 复用轻量元数据结构（不带 raw_content）
- **Location:** src-tauri/src/models/note.rs:72
- **Exported:** No

