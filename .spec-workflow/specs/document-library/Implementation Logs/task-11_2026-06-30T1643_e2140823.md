# Implementation Log: Task 11

**Summary:** 事后补录（2026-07-01，/ruiqin 补 v0.2 log）：全库搜索（FTS5 trigram tokenizer，原生支持中文短语）+ snippet 高亮 + bm25 排序。关键修复：index_vault 末尾 `INSERT INTO notes_fts(notes_fts) VALUES('rebuild')`（commands/index.rs:225），解决 contentless FTS 表历史上从未与 notes 同步的问题——'rebuild' 从 notes 全量重读重建，一次解决「FTS 空表 + 旧数据残留」。命令 search_notes（commands/search.rs）JOIN notes 取 vault 过滤与元数据，snippet(notes_fts,1,...) 取正文片段（建表顺序 title(0)/raw_content(1)/tags(2)，取列 1），返回 SearchResult 元数据 + snippet + rank；严格不带 raw_content 全文（性能红线）。sanitize_query 去引号防 phrase 语法破坏，空串返回空结果。前端入口当前落点：CommandPalette.tsx（Input.Search + 200ms debounce + api.searchNotes → openNoteFromMeta 跳转），原 LibraryPage.tsx 搜索框随 v0.2 Obsidian-first 重构下线（详见 task-7 注记）。

**Timestamp:** 2026-06-30T16:43:52.438Z
**Log ID:** e2140823-9cae-4dcd-a08d-7466c5faccc9

---

## Statistics

- **Lines Added:** +220
- **Lines Removed:** -10
- **Files Changed:** 6
- **Net Change:** 210

## Files Modified
- src-tauri/src/commands/index.rs
- src-tauri/src/commands/search.rs
- src-tauri/src/models/note.rs
- frontend/src/components/CommandPalette.tsx
- frontend/src/types/index.ts
- frontend/src/api/index.ts

## Files Created
_No files created_

---

## Artifacts

### API Endpoints

#### GET search_notes
- **Purpose:** 全库搜索：FTS5 trigram MATCH + snippet 高亮 + bm25 排序 + vault 过滤。返回元数据+snippet+rank，不带 raw_content 全文（性能红线）
- **Location:** src-tauri/src/commands/search.rs:27
- **Request Format:** vault_id: String, query: String (自然语言短语，中文直接可用), limit: Option<i64> (默认 50)
- **Response Format:** Vec<SearchResult> { id, rel_path, file_name, title, note_type, date_iso, tags: Vec<String>, snippet (带 <b> 高亮), rank (bm25, 越小越相关) }

### Components

#### CommandPalette
- **Type:** React
- **Purpose:** 命令面板（Ctrl/⌘+P 触发）：Input.Search + 200ms debounce → api.searchNotes → List 展示 SearchResult → 点击 openNoteFromMeta 跳转。当前搜索入口落点（原 LibraryPage 搜索框已随 v0.2 重构下线，详见 task-7 注记）
- **Location:** frontend/src/components/CommandPalette.tsx
- **Props:** {}（无 props，状态自管：paletteOpen 来自 tabs store，vault 来自 vault store）
- **Exports:** default

### Functions

#### sanitize_query
- **Purpose:** 清洗 FTS5 查询串：去掉双引号防破坏 phrase "…" 语法，trim 后返回。空串表示无有效查询（调用方返回空结果）
- **Location:** src-tauri/src/commands/search.rs:18
- **Signature:** fn sanitize_query(q: &str) -> String
- **Exported:** No

### Classes

#### SearchResult
- **Purpose:** FTS5 搜索命中 DTO：轻量元数据 + snippet（命中关键词 <b> 高亮，取自 raw_content 列）+ bm25 rank。关键不带 raw_content 全文（性能红线）
- **Location:** src-tauri/src/models/note.rs:56
- **Exported:** No

