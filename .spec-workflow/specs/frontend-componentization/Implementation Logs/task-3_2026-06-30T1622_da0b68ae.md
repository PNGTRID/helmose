# Implementation Log: Task 3

**Summary:** 事后补录(2026-07-01,/ruiqin 补 log): 新建 frontend/src/utils/tree.tsx(注意是 .tsx: fileIcon 返回 JSX,故需 tsx 扩展),从 components/FilePanel.tsx 迁出三个树域纯函数 + SortMode 类型: makeCompare(构造 name/natural 排序比较器)、childDirs(取直接子目录)、fileIcon(按扩展名返回 md/图片/其他图标 JSX)。FilePanel 已改为 `import { childDirs, fileIcon, makeCompare, type SortMode } from "../utils/tree"`。纯重构,函数逻辑/签名不变,对应 requirements 4.1(纯函数集中)与 4.2(原组件改为 import)。

**Timestamp:** 2026-06-30T16:22:02.051Z
**Log ID:** da0b68ae-e96b-414c-98aa-1e18dcfaa4ba

---

## Statistics

- **Lines Added:** +35
- **Lines Removed:** -30
- **Files Changed:** 2
- **Net Change:** 5

## Files Modified
- frontend/src/components/FilePanel.tsx

## Files Created
- frontend/src/utils/tree.tsx

---

## Artifacts

### Functions

#### fileIcon
- **Purpose:** 按文件名给出图标节点(md/markdown → FileTextOutlined 紫色;图片扩展名 png/jpg/jpeg/gif/svg/webp → FileImageOutlined 绿色;其他 → FileOutlined)。从 FilePanel 迁入,逻辑不变。返回 JSX,故文件用 .tsx 而非 .ts。
- **Location:** frontend/src/utils/tree.tsx:9
- **Signature:** (name: string) => ReactNode
- **Exported:** No

#### makeCompare
- **Purpose:** 构造排序比较器: mode='name' → 纯字典序 localeCompare(sensitivity: base);mode='natural' → 数字感知 localeCompare(numeric: true,如 2 < 10)。从 FilePanel 迁入,逻辑不变。
- **Location:** frontend/src/utils/tree.tsx:19
- **Signature:** (mode: SortMode) => (a: string, b: string) => number
- **Exported:** No

#### childDirs
- **Purpose:** 取某目录的直接子目录(不含更深层级): 根目录("") → 顶层无 '/' 的项;子目录 → 以 dir + '/' 为前缀且剩余部分不含 '/' 的项。从 FilePanel 迁入,逻辑不变。
- **Location:** frontend/src/utils/tree.tsx:28
- **Signature:** (dir: string, dirs: string[]) => string[]
- **Exported:** No

